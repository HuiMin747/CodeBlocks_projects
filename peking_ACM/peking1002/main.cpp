#include <iostream>

using namespace std;

char    tele[20];

char *  standardize(char * p)
{
    char * pp=p;
    for(int i=0;i<20;i++)
    {
        if(pp[i]=='-')
        {
            ;    //skip the hyphen
        }
        else
        {
            switch(pp[i])
            {
            case 'A','B','C':

            }
        }
    }
}

int main()
{

    while(cin>>)
    return 0;
}
